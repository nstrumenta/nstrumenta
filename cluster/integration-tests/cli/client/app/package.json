{
  "name": "nodejs-client",
  "version": "1.0.0",
  "description": "nodejs client tests",
  "main": "index.js",
  "scripts": {
    "test-from-docker-compose": "npx wait-on http://server:5999 && echo 'wait-on server:5999 complete' && jest --verbose=true --detectOpenHandles",
    "test": "jest --detectOpenHandles"
  },
  "author": "Tyler Bryant",
  "license": "UNLICENSED",
  "dependencies": {
    "wait-on": "^6.0.1",
    "zx": "^6.1.0"
  },
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.4"
  }
}