{
  "name": "browser-client",
  "version": "0.0.1",
  "description": "browser-client integration tests",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test-from-docker-compose": "npx wait-on ${WAIT_ON_URL} && playwright test"
  },
  "author": "nstrumenta",
  "license": "UNLICENSED",
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.3",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "nstrumenta": "file:../../../../"
  }
}
