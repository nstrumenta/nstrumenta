import { DateAsQueryParamPipe } from './date-as-query-param.pipe';

describe('DateAsQueryParamPipe', () => {
  it('create an instance', () => {
    const pipe = new DateAsQueryParamPipe();
    expect(pipe).toBeTruthy();
  });
});
