{
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}",
    "requireCleanWorkingDir": true
  },
  "npm": {
    "publish": false
  },
  "github": {
    "release": false
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "infile": "CHANGELOG.md"
    }
  },
  "hooks": {
    "after:bump": "sed -i \"s/export const version = '.*';/export const version = '${version}';/\" lib/version.ts && sed -i \"s/version='.*'/version='${version}'/\" python/setup.py && (cd frontend && npm version ${version} --no-git-tag-version) && (cd server/app && npm version ${version} --no-git-tag-version) && (cd agent-admin-page && npm version ${version} --no-git-tag-version) && (cd vscode-extension && npm version ${version} --no-git-tag-version) && (cd functions && npm version ${version} --no-git-tag-version) && (cd data-job-runner/app && npm version ${version} --no-git-tag-version) && git add . && npm run build",
    "after:release": "cd python && rm -rf dist/* && python3 setup.py sdist bdist_wheel && twine upload dist/*"
  }
}
