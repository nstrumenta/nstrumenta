{
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}",
    "requireCleanWorkingDir": true
  },
  "npm": {
    "publish": true
  },
  "github": {
    "release": false
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "infile": "CHANGELOG.md"
    }
  },
  "hooks": {
    "after:bump": "sed -i \"s/export const version = '.*';/export const version = '${version}';/\" lib/version.ts && sed -i \"s/version='.*'/version='${version}'/\" python/setup.py",
    "after:release": "cd python && python3 setup.py sdist bdist_wheel && twine upload dist/*"
  }
}
