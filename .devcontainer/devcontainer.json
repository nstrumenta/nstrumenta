{
  "build": {
    "dockerfile": "../Dockerfile"
  },
  "customizations": {
    // Add the IDs of extensions you want installed when the container is created.
    "extensions": [
      "hashicorp.terraform"
    ]
  },
  "runArgs": [
    "--privileged"
  ]
}